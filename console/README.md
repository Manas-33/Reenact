# Reenact Console

Hosted viewer and debugger for recorded trajectories: browse runs, inspect any
step, diff two runs, step through a failure, and fork-and-compare. Built with
Next.js and TypeScript, read-only over synced Postgres until the fork engine
introduces the first Python service.
