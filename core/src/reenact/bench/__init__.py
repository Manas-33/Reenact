"""Benchmark harness: regenerates the metrics table (replay determinism, catch
rate and false-positive rate, judge agreement, recording overhead), reports
each number with its denominator, and runs in CI so the numbers never go stale.
"""
